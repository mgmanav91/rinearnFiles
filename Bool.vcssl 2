coding UTF-8;

# PRAGMA COMPILE    REFERRAL
# PRAGMA RESERVE    ALL
# PRAGMA END

import Text;

/**
 * <p>
 * このライブラリ system.Bool は、bool型の補助機能を提供します。
 * </p>
 * 
 * @author  松井文宏 - Fumihiro Matsui ( RINEARN )
 * @license Public domain ( CC0 )
 */



/**
 * 文字列の中身がbool型に変換可能か判定します。
 *
 * @param s 判定内容
 * @return 判定結果（trueであれば変換可能）
 */
public bool isBool(string s){
	if(s=="true" || s=="false"){
		return true;
	}else{
		return false;
	}
}



/**
 * bool型の複数の値を、配列にまとめて返します。
 *
 * @param args 配列にまとめたい値（可変長引数）
 * @return 引数の値をまとめた配列
 */
public bool[] toBoolArray(... bool args[]){
	return args;
}


